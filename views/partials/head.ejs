<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <!-- <link rel="stylesheet" href="../style.css"> -->
  <title>unit2Project</title>
  <style>
    .flex-container {
      /* display: flex;
      flex-direction: row-reverse;
      flex-wrap: wrap-reverse;
      justify-content: center;
      column-gap: 5px;
      padding: 5px; */
      /* background-color: #333; */
      display: grid;
      grid-template-columns: repeat(4, max(300px, 300px));
      justify-content: center;
      grid-gap: 10px;
      grid-auto-flow: dense;
    }
    .flex-item {
      position: relative;
      text-align: center;
    }
    /* .flex-item:hover {
      border-radius: 30px;
      opacity: 0.5;
    } */
    .flex-item-title {
      position: absolute;
      top: 0;
      bottom: 0;
      left: 0;
      right: 0;
      visibility: hidden;
      opacity: 0;
    }
    .gallery-image {
      position: relative;
      vertical-align: top;
      height: 300px;
      width: 300px;
    }
    .gallery-image:hover {
      border-radius: 30px;
      opacity: 0.5;
    }
    .show-image {
      height: 500px;
      width: 500px;
    }
  </style>
</head>
